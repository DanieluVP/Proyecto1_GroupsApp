apiVersion: apps/v1
kind: Deployment
metadata:
  name: groups-service
  namespace: groupsapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: groups-service
  template:
    metadata:
      labels:
        app: groups-service
    spec:
      containers:
        - name: groups-service
          image: groupsapp/groups-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3002
            - name: grpc
              containerPort: 5002
          env:
            - name: PORT
              value: "3002"
            - name: GRPC_PORT
              value: "5002"
            - name: DB_HOST
              value: postgres
            - name: DB_NAME
              value: groupsapp_groups
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: groupsapp-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: groupsapp-secrets
                  key: JWT_SECRET
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: NODE_ENV
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: KAFKA_BROKERS
            - name: CONSUL_HOST
              valueFrom:
                configMapKeyRef:
                  name: groupsapp-config
                  key: CONSUL_HOST
            - name: SERVICE_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3002
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3002
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: groups-service
  namespace: groupsapp
spec:
  selector:
    app: groups-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
    - name: grpc
      port: 5002
      targetPort: 5002
